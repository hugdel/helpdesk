<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="ticket_view_create_partner_form" model="ir.ui.view">
        <field name="name">helpdesk.ticket.view.form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk_mgmt.ticket_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='assign_to_me']" position="after">
                <field name="show_create_partner" invisible="1"/>
                <button string="Create partner"
                        name="create_res_partner"
                        type="object"
                        attrs="{'invisible' : [('show_create_partner', '=', False)]}"/>
            </xpath>

            <xpath expr="//field[@name='partner_email']" position="after">
                <field name="partner_phone"/>
                <field name="partner_address"/>
                <field name="partner_address_invoice"/>
            </xpath>

        </field>
    </record>

</odoo>
